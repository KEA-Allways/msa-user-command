spring:
  application:
    name: user-command-service
  datasource:
    url: jdbc:mysql://3.80.230.96:3306/USER_BLOG_DB?allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234
    jpa:
      database: mysql
      open-in-view: false
      show-sql: true
      hibernate:
        properties:
          hibernate:
            format_sql: true

  # 로깅 설정
  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type: trace

  # 서버 설정
server:
  port: 8083

# Eureka 클라이언트 설정
eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${server.port}}
    hostname: 44.201.129.31 #
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://3.213.139.105:8761/eureka #


jwt:
  key:
    #스프링부트 마스터하고싶다
    access: 7Iqk7ZSE66eB67aA7Yq4IOuniOyKpO2EsO2VmOqzoOyLtuuLpA==
    #스프링부트 마스터하고싶다 진짜로
    refresh: 7Iqk7ZSE66eB67aA7Yq4IOuniOyKpO2EsO2VmOqzoOyLtuuLpCDsp4Tsp5zroZw=
  max-age:
    access: 604800 # 60 * 30 테스트를 위해 원래는 1800이던걸 30으로 줄여놨습니다.
    refresh: 604800 # 60 * 60 * 24 * 7

